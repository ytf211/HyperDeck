<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.HyperDeck" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">?attr/colorSurface</item>
        <item name="windowBackground">?attr/colorSurface</item>
    </style>

    <style name="Theme.HyperDeck.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.HyperDeck</item>
    </style>
</resources>
